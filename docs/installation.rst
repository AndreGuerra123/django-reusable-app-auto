============
Installation
============

At the command line::

    $ easy_install django-reusable-app-auto

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-reusable-app-auto
    $ pip install django-reusable-app-auto
